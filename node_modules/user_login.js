// 用户
const mongoose = require('mongoose');

const userSchema = {
    name: String,
    email: String,
    password: String,
    AssociationID: Number
};

let USER = mongoose.model('USER', userSchema);

models.exports(USER);